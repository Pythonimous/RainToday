[pytest]
# Playwright-specific pytest configuration
testpaths = tests/e2e
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Playwright browser configuration
# Use --browser option to specify: chromium, firefox, webkit
# Use --headed to run in headed mode (see the browser)
# Use --slowmo to slow down operations
